{
  "name": "server",
  "version": "1.0.0",
  "description": "backend server for mern stack authentication and authorisation ",
  "main": "index.js",
  "scripts": {
    "start": "node --watch-path ./ index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vidit Goel",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
